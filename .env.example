# =============================================================================
# Site Orchestrator — environment template
# Copy this file to .env and fill in real values. Never commit .env to git.
# =============================================================================

# -----------------------------------------------------------------------------
# Salesforce (UAT sandbox)
# -----------------------------------------------------------------------------
SF_USERNAME=your-uat-username
SF_PASSWORD=your-uat-password
SF_SECURITY_TOKEN=your-uat-security-token
# Standard sandbox: test | Custom My Domain sandbox: yourorg--uat.sandbox
SF_DOMAIN=symphonyinfra--uat.sandbox

# -----------------------------------------------------------------------------
# Geocoding (US Census + Nominatim — shared by orchestrator ingest and classifier)
# Set GEOCODER=auto (default), census, or nominatim.
# -----------------------------------------------------------------------------
GEOCODER=auto
GEOCODER_USER_AGENT=site-orchestrator/1.0 (cell-site imagery pipeline)

# Urbanicity search radii use US ZCTA populations from a CSV (zip,population columns).
# Works nationwide — point at a full Census ZCTA export or a regional subset.
# Example regional file: data/zip_populations.csv (Milwaukee sample)
# ZIP_POPULATION_CSV=data/zip_populations.csv

# Geocode Salesforce Site__c rows missing Site_Latitude__c/Site_Longitude__c during dedupe prefetch.
# Set to 0 to disable (spatial scoring falls back to address-only for those records).
SF_GEOCODE_MISSING=1

# Salesforce upload defaults (sf_upload.csv template + API create)
# Carrier_Leasing_Source__c auto-generates as JF_PermitScraping_{mon}{yy} (e.g. JF_PermitScraping_jul26)
# SF_CARRIER_LEASING_SOURCE=JF_PermitScraping_jul26
# SF_OWNER_ID=0056O00000EpUOgQAN
SF_DEFAULT_VERIFIED_SOURCE=Permitting Data
# SF_DEFAULT_PROPERTY_TYPE=Commercial
# Override API field names when your org uses different Site__c custom fields:
# SF_FIELD_CARRIER_LEASING_SOURCE=Carrier_Leasing_Source__c
# SF_FIELD_SITE_STREET=Street__c

# -----------------------------------------------------------------------------
# Classifier API keys
# -----------------------------------------------------------------------------
ANTHROPIC_API_KEY=sk-ant-your-key-here
NEARMAP_API_KEY=your-nearmap-key
GEMINI_API_KEY=your-gemini-key

# -----------------------------------------------------------------------------
# Pipeline mode flags
# -----------------------------------------------------------------------------
NEARMAP_TIERED=1
BIFURCATED_AI=1
GEMINI_ONLY=0
TOWER_ONLY=0
ZOOM_STAGE=1
WIDE_AOI_STAGE=1
NAIP_ONLY=0

# -----------------------------------------------------------------------------
# AI model settings
# -----------------------------------------------------------------------------
CLAUDE_MODELS=claude-sonnet-4-6,claude-haiku-4-5-20251001
CLAUDE_ESCALATION_MODEL=claude-sonnet-4-6
GEMINI_MODEL=gemini-3-flash-preview
# Thinking tokens for vision (0=off). Empty/auto → 1024 non-lite, 0 for *-lite*.
GEMINI_THINKING_BUDGET=1024
GEMINI_SOLO_CELL_CONF=0.90
GEMINI_CLAUDE_BOX_IOU=0.20
# Pilot (150 sites, NAIP tower screening): gemini-3.1-flash-lite @ GEMINI_DELAY_S=30
GEMINI_DELAY_S=30
GEMINI_RETRIES=6
GEMINI_RETRY_BASE_S=20
CLAUDE_DELAY_S=12
CHIP_SIZE_M=250
# Pin far from geocoded address → classify on address (see enrichment/pin_address.py)
PIN_ADDRESS_MISMATCH_M=50
# FCC/TowerSource: search to 500m; ≤25m confident; extended needs uniqueness/address affinity
PROXIMITY_MAX_M=500
PROXIMITY_CONFIDENT_M=25
PROXIMITY_AMBIGUITY_GAP_M=75
PROXIMITY_ADDRESS_AFFINITY_M=80
# Post-apply / post-classify spot audit of ready candidates
SPOT_AUDIT_RATE=0.10
SPOT_AUDIT_MIN=3
SPOT_AUDIT_MAX=25
NAIP_WIDE_CHIP_M=500
TIER_CONF_HIGH=0.75
TIER_CONF_MEDIUM=0.6
# Rooftop cell_equipment_confidence bar for Nearmap early-stop / skip Claude
ROOFTOP_CELL_CONF_MIN=0.75
# Rooftop on NAIP older than this requires Nearmap unless conf >= override.
NAIP_MAX_AGE_YEARS=2
NAIP_AGE_HIGH_CONF_OVERRIDE=0.85

# -----------------------------------------------------------------------------
# Azure SQL (FCC / TowerSource proximity — enrichment + orchestrator snap)
# -----------------------------------------------------------------------------
# AZURE_SQL_SERVER=your-server.database.windows.net
# AZURE_SQL_DATABASE=your_db
# AZURE_SQL_DRIVER=ODBC Driver 18 for SQL Server
# AZURE_SQL_ODBC_AUTHENTICATION=
# ORCHESTRATOR_PROXIMITY_MAX_M=25
